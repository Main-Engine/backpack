import { TextStyle } from 'react-native'
import { Theme } from 'utils/theme'
import { StyledTypographyProps } from './Typography.types'

export declare const createTypographyStyles: (
  theme: Theme,
  props: StyledTypographyProps
) => {
  text: TextStyle
}
