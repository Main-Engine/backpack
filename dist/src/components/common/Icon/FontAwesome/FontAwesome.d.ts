import type { StyledIconProps } from '../Icon.types'

export declare const FontAwesome: ({
  name,
  color,
  colorKey,
  size,
  style,
  weight
}: StyledIconProps) => import('react/jsx-runtime').JSX.Element
