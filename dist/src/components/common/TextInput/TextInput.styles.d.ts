import { TextStyle } from 'react-native'
import { Theme } from 'utils/theme'
import { StyledTextInputProps } from './TextInput.types'

export declare const createTextInputStyles: (
  theme: Theme,
  props: StyledTextInputProps
) => {
  input: TextStyle
}
