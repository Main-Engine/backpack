import { ViewStyle } from 'react-native'
import { Theme } from 'utils/theme'
import type { StyledBoxProps } from './Box.types'

export declare const createBoxStyles: (
  theme: Theme,
  props: StyledBoxProps
) => {
  box: ViewStyle
}
